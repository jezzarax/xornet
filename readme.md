# WIP - some ggml playground of mine

# running


- get `train_xornet.py` and `conver_xornet.py` to run to the end
- build ggml model runner

```
mkdir build && cd build
cmake ..
make
./xornet ../models/xornet.gguf
```